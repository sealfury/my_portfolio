import React from "react";
import { Container } from "semantic-ui-react";

const Projects = () => {
    return (
        <Container>
            <h1 id="projects-header">My Projects</h1>
            <p>
                Let's maybe look into potential templates on semantic-UI for this part 
                so the links to the github repo seem a bit more appealing?
            </p>
        </Container>
    );
};

export default Projects